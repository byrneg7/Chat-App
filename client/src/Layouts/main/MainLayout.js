import React from 'react';

import './mainLayout.scss'

const MainLayout = ({children}) => {
    return(
        <div>
            main layout
            {children}
        </div>
    )
};

export default MainLayout;